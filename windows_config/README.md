# Windows configuration

Focused, reproducible configuration for the Windows workstation lives here.

- `handy_model_switch/` registers `Win+Shift+F` to switch Handy between the
  Voxtral 4B automatic-language profile and the Granite 2B Japanese profile,
  and adds Caps Lock plus Handy profile indicators to the taskbar notification
  area.
